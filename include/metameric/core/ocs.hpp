#pragma once

namespace met {
  // OCS generation
  // 1. Generate n random sampled unit vectors k||[0, 1]^3||
  // 2. Compute r_opt = {0, 1} depending on the sign of dot(k, s(lambda))
  // 3.  
} // namespace met